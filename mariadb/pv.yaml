apiVersion: v1
kind: PersistentVolume
metadata:
  name: mariadb-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 192.168.100.41 # Replace with the IP address of your NFS server
    path: /data/mariadb # Replace with the actual path on the NFS server
